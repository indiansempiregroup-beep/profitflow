import { designTokens } from '@profitflow/ui';

export const theme = {
  colors: designTokens.colors,
  spacing: designTokens.spacing,
  radius: designTokens.radius,
  typography: designTokens.typography,
  shadow: designTokens.shadow,
};
