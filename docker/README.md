# Docker

Use Docker Compose to run the local development stack for the server and its dependencies.

## Commands

- docker compose -f docker/docker-compose.yml up --build
- docker compose -f docker/docker-compose.yml down
